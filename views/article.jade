extends layout
block content
  p
    span
      a.edit(href='/edit/#{post.name}/#{post.createTime.day}/#{post.title}') 编辑
    span
      a.edit(href='/remove/#{post.name}/#{post.createTime.day}/#{post.title}') 删除
  p.info
    | 作者：
    a(href='/u/#{post.name}')= post.name
    | | 日期：#{post.createTime.minute}
  p!= post.content