extends layout
block content
  each post in posts
    p
      h2
        a(href='/u/#{post.name}/#{post.createTime.day}/#{post.title}')= post.title
    p.info
      | 作者：
      a(href='/u/#{post.name}')= post.name
      | | 日期：#{post.createTime.minute}
    p!= post.content